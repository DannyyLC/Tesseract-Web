# Variables de entorno NO sensibles del servicio Cloud Run `agents`.
#
# Hoy está vacío a propósito: `agents` solo recibe secretos (AGENTS_INTERNAL_SECRET,
# ANTHROPIC_API_KEY, OPENAI_API_KEY), que van por `--set-secrets` en cloudbuild.yaml.
# El archivo existe para que `--env-vars-file` tenga a qué apuntar y para que cualquier
# variable plana futura (LOG_LEVEL, por ejemplo) tenga un lugar evidente donde ir.
#
# Ojo: ANTHROPIC_API_KEY y OPENAI_API_KEY no aparecen en el código — las lee litellm
# directo del entorno. No se pueden derivar de un grep; esta declaración es su único
# registro.
#
#   pnpm run env:diff agents
{}
