# Role-Based Access Control (RBAC)

This document describes the permissions and role management system within Tesseract/Fractal.

## Roles Defined

(To be written: List of available roles in the system (e.g., Admin, User, Viewer) and their general purpose.)

## Permissions Matrix

(To be written: Detailed breakdown of what actions each role is permitted to perform (e.g., `workflows:read`, `workflows:execute`).)

## Implementation Details

(To be written: How roles and permissions are enforced in the backend (Guards/Middleware) and frontend (UI rendering logic).)
